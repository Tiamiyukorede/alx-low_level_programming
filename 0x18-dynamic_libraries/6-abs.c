#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 * @t: THE integer yo be computed
 * Return: return the absolute value
 */
int _abs(int t)
{
	if (t >= 0)
		return (t);
	else
		return (-t);
}
